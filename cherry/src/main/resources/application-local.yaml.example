# 复制为 application-local.yaml 后按需修改（application-local.yaml 已 gitignore，勿提交密码）
spring:
  datasource:
    password: your-password
    # url: jdbc:mysql://127.0.0.1:3306/cherry_oj?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    # username: root

# JWT 与 Cookie（也可用环境变量 APP_JWT_SECRET / APP_JWT_EXPIRATION_MS / APP_JWT_COOKIE_NAME）
# app:
#   jwt:
#     secret: "至少-32-字节-的随机密钥-生产务必覆盖"
#     expiration-ms: 86400000
#     cookie-name: cherry_jwt
#     cookie-secure: false
#
# 远程判题服务（cherry-judge）
# app:
#   judge:
#     enabled: true
#     base-url: http://127.0.0.1:6060
#     connect-timeout-ms: 2000
#     poll-interval-ms: 200
#     max-wait-ms: 15000
